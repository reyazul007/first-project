

import{AppBar, Toolbar, styled} from '@mui/material';

const container = styled(AppBar)`
     background: #060606;
`
const Header = () => {
    return (
       <container>
           <Toolbar>
        
           </Toolbar>
       </container>
    )
}

export default Header;